pipeline {
    agent {
        docker {
            image 'androidsdk/android-29'
        }
    }
    stages {
        stage('Build') {
            steps {
                echo 'Running build'
            }
        }
        stage('Test') {
            steps {
                echo 'Running Test'
                sh './gradlew --no-daemon --debug :app:src:androidTest:java:app.msda.qna:PermissionUT'
            }
            post {
                always {
                    echo 'Running post-test'
                }
            }
        }
        stage('Deliver') {
            steps {
                echo 'Running Deliver'
            }
        }
    }
}